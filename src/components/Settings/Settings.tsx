import React from 'react';
import '../../App.scss'
import './Settings.scss'


const Settings = () => {
   return (
      <section className="settings section">
         <div className="settings__text section__text">Settings</div>
      </section>
   );
};

export default Settings;