import React from 'react';
import './Dialogs.scss'
import '../../App.scss'

const Dialogs = () => {
   return (
      <section className="dialogs section">
         <div className="dialogs__text section__text">Dialogs</div>
      </section>
   );
};

export default Dialogs;